const outerBox = {
  marginTop: 8,
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
};

const loginIcon = { m: 1, bgcolor: "primary.main" };

const googleButton = {
  color: " #ccc",
  backgroundColor: "rgba(0, 183, 255, 0.795)",
};

// eslint-disable-next-line import/no-anonymous-default-export
export default { outerBox, loginIcon, googleButton };
